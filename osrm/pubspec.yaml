name: osrm
description: Open Source Routing Machine (OSRM) client for Dart.
version: 0.0.2
repository: https://github.com/physia/kflutter

environment:
  sdk: ^3.0.2

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  async: ^2.11.0
  http: ^1.1.0
  recase: ^4.1.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
